version: 2

config:
  log_level: INFO
  output_dir: ${OUTPUT_DIR}
  memory_limit: 1GB
  # state_file: ${STATE_FILE}
  show_graph: False
  show_stacktrace: true
  parameter_defaults:
    STUDENT_ID_NAME: 'edFi_studentUniqueID' # default to the column added by the apply_xwalk package of student ID xwalking feature
    OUTPUT_DIR: output/
    POSSIBLE_STUDENT_ID_COLUMNS: Local ID,State ID
    DESCRIPTOR_NAMESPACE: uri://ed-fi.org
    TEST_TYPE: aMath
  macros: >
      {# Extract season from column name. If it's a season-to-season column, use the second season. #}
      {%- macro extract_season(col) -%}
        {%- set seasons = ['fall', 'winter', 'spring', 'screening_period_4', 'screening_period_5'] -%}
        {%- set search_str = col.split('_to_', 1)[1] if '_to_' in col else col -%}
        {%- set found = [] -%}
        {%- for s in seasons -%}
          {%- if found | length == 0 -%}
            {%- set prefix = s + '_' -%}
            {%- if (search_str | length >= prefix | length and search_str[:prefix | length] == prefix) or search_str == s -%}
              {%- set _ = found.append(s) -%}
            {%- endif -%}
          {%- endif -%}
        {%- endfor -%}
        {{ found[0] if found | length > 0 else '' }}
      {%- endmacro -%}

      {# Remove season from column name to standardize for the pivot. #}
      {%- macro remove_season(col, season) -%}
        {%- if season | length > 0 -%}
          {%- if '_to_' in col -%}
            {{ col.replace('_to_' + season, '') }}
          {%- else -%}
            {{ col[season | length + 1:] if col.startswith(season + '_') else ('' if col == season else col) }}
          {%- endif -%}
        {%- else -%}
          {{ col }}
        {%- endif -%}
      {%- endmacro -%}

      {# Remove test type from the column name ("a_math" or "a_reading") #}
      {%- macro remove_test_type(col, test_type) -%}
        {{ col.replace(test_type + "_", "") }}
      {%- endmacro -%}

{% if "${TEST_TYPE}" == "aReading" %}
  {% set test_type = "a_reading" %}
{% else %}
  {% set test_type = "a_math" %}
{% endif %}

{% set custom_descriptors = [
  "assessmentCategoryDescriptors",
  "assessmentPeriodDescriptors",
  "assessmentReportingMethodDescriptors",
  "performanceLevelDescriptors",
  "resultDatatypeTypeDescriptors",
] %}

sources:

  assessments:
    file: ./seeds/assessments.csv
    header_rows: 1

  # xwalks
  academicSubjectDescriptors:
    file: ./seeds/xwalks/academicSubjectDescriptors.csv
    header_rows: 1
  gradeLevelDescriptors:
    file: ./seeds/xwalks/gradeLevelDescriptors.csv
    header_rows: 1

  # Custom Descriptors
  {% for descr in custom_descriptors %}
  {{ descr }}:
    file: ./seeds/custom_descriptors/{{ descr }}.csv
    header_rows: 1
  {% endfor %}

  input:
    file: ${INPUT_FILE}
    header_rows: 1

transformations:
  input:
    source: $sources.input
    operations: []

  cleaned_input:
    source: $transformations.input
    operations:
      - operation: duplicate_columns
        columns:
          ${STUDENT_ID_NAME}: student_unique_id
      
      - operation: snake_case_columns

      - operation: melt
        id_vars: [assessment, assessment_language, district, school, local_id, state_id, fast_id, grade, student_unique_id]

      - operation: add_columns
        columns:
          season: "{% raw %}{{ extract_season(melt_variable) }}{% endraw %}"
          test_type: "{{ test_type }}"
          cleaned_variable: "{% raw %}{{remove_test_type(remove_season(melt_variable, season), test_type)}}{% endraw %}"

      - operation: filter_rows
        query: season == ''
        behavior: exclude

      - operation: modify_columns
        columns:
          melt_value: "{% raw %}{{ melt_value|replace('N/A*', '') }}{% endraw %}"

      - operation: pivot
        cols_by: cleaned_variable
        rows_by: [assessment, assessment_language, district, school, local_id, state_id, fast_id, grade, student_unique_id, season]
        values: melt_value

      - operation: map_values
        column: season
        mapping:
          fall: Fall
          winter: Winter
          spring: Spring
          screening_period_4: Screening Period 4
          screening_period_5: Screening Period 5

      - operation: filter_rows
        query: final_date == ''
        behavior: exclude

      - operation: duplicate_columns
        columns:
          final_date: administration_date

      - operation: add_columns
        columns:
          assessmentIdentifier: ${TEST_TYPE}
          namespace: "uri://fastbridge.org"
          descriptor_namespace: ${DESCRIPTOR_NAMESPACE}
          school_year: ${API_YEAR}

      - operation: combine_columns
        columns:
          - administration_date
          - student_unique_id
          - season
        new_column: studentAssessmentIdentifier
        separator: '-'

      - operation: modify_columns
        columns:
          studentAssessmentIdentifier: "{%raw%}{{ md5(studentAssessmentIdentifier) }}{%endraw%}"

      - operation: date_format
        column: administration_date
        from_format: "%m/%d/%Y"
        to_format: "%Y-%m-%d"

      # the Advanced risk level category has different abbreviations in different files
      - operation: map_values
        column: risk_level
        mapping:
          AD: Advanced
          AV: Advanced
          LI: Advanced

      - operation: join
        sources:
          - $sources.gradeLevelDescriptors
        join_type: left
        left_keys: 
          - assessmentIdentifier
          - grade
        right_keys:
          - assessmentIdentifier
          - original_grade
        right_keep_columns:
        - gradeLevelDescriptor

      # aMath/aReading have Quantile/Lexile columns, respectively.
      # Make sure they both exist. One will always be ignored.
      - operation: add_columns
        columns:
        {% if test_type == "a_reading" %}
            quantile: ""
        {% else %}
            lexile: ""
        {% endif %}

  # Generate grade level JSON for assessments
  grade_level_descriptors:
    source: $sources.gradeLevelDescriptors
    operations:
      - operation: add_columns
        columns:
          grade_json: >
            {%raw-%}
            {
              "gradeLevelDescriptor": "{{gradeLevelDescriptor}}"
            },
            {%-endraw%}
      - operation: modify_columns
        columns:
          grade_json: "{%raw%}{{grade_json | replace('\n', '')-}}{%endraw%}"
      # group and aggregate:
      - operation: group_by
        group_by_columns:
          - assessmentIdentifier
        create_columns:
          grade_json: agg(grade_json,)
      # there will be a trailing comma, want to get rid of that
      - operation: modify_columns
        columns:
          grade_json: "{%raw%}{{value.rstrip().rstrip(',')}}{%endraw%}"

  # Generate performance level JSON for assessments
  performance_level_descriptors:
    source: $sources.performanceLevelDescriptors
    operations:
      - operation: add_columns
        columns:
          base_namespace: "uri://fastbridge.org"
          descriptor_namespace: ${DESCRIPTOR_NAMESPACE}
          performance_level_json: >
            {%raw-%}
            {
              "assessmentReportingMethodDescriptor": "{{base_namespace}}/AssessmentReportingMethodDescriptor#Risk Level",
              "performanceLevelDescriptor": "{{base_namespace}}/PerformanceLevelDescriptor#{{codeValue}}",
              "resultDatatypeTypeDescriptor": "uri://ed-fi.org/ResultDatatypeTypeDescriptor#Level"
            },
            {%-endraw%}
      - operation: modify_columns
        columns:
          performance_level_json: "{%raw%}{{performance_level_json | replace('\n', '')-}}{%endraw%}"
      # group and aggregate:
      - operation: group_by
        group_by_columns:
          - assessmentIdentifier
        create_columns:
          performance_level_json: agg(performance_level_json,)
      # there will be a trailing comma, want to get rid of that
      - operation: modify_columns
        columns:
          performance_level_json: "{%raw%}{{value.rstrip().rstrip(',')}}{%endraw%}"

  # Generate assessment period JSON for assessments
  assessment_period_descriptors:
    source: $sources.assessmentPeriodDescriptors
    operations:
      - operation: add_columns
        columns:
          period_json: >
            {%raw-%}
            {
              "assessmentPeriodDescriptor": "{{namespace}}#{{codeValue}}"
            },
            {%-endraw%}
      - operation: modify_columns
        columns:
          period_json: "{%raw%}{{period_json | replace('\n', '')-}}{%endraw%}"
      # group and aggregate:
      - operation: group_by
        group_by_columns:
          - assessmentIdentifier
        create_columns:
          period_json: agg(period_json,)
      # there will be a trailing comma, want to get rid of that
      - operation: modify_columns
        columns:
          period_json: "{%raw%}{{value.rstrip().rstrip(',')}}{%endraw%}"



{% for descr in custom_descriptors %}
  {{ descr }}_unique:
    source: $sources.{{ descr }}
    operations:
      - operation: distinct_rows
        columns:
          - codeValue
{% endfor %}

  assessments:
    source: $sources.assessments
    operations:
      - operation: join
        sources:
          - $transformations.performance_level_descriptors
        join_type: inner
        left_key: assessmentIdentifier
        right_key: assessmentIdentifier
      - operation: join
        sources:
          - $transformations.grade_level_descriptors
        join_type: inner
        left_key: assessmentIdentifier
        right_key: assessmentIdentifier
      - operation: join
        sources:
          - $transformations.assessment_period_descriptors
        join_type: inner
        left_key: assessmentIdentifier
        right_key: assessmentIdentifier  

destinations:
  assessments:
    source: $transformations.assessments
    template: ./templates/assessments.jsont
    extension: jsonl
  studentAssessments:
    source: $transformations.cleaned_input
    template: ./templates/studentAssessments.jsont
    extension: jsonl

{% for descr in custom_descriptors %}
  {{ descr }}:
    source: $transformations.{{ descr }}_unique
    template: ./templates/descriptors.jsont
    extension: jsonl
    linearize: True
{% endfor %}
