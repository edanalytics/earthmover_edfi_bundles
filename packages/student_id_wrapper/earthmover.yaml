version: 2

config:
  output_dir: ${OUTPUT_DIR}
  log_level: INFO
  show_stacktrace: True
  show_graph: False
  parameter_defaults:
    OUTPUT_DIR: output
    ASSESSMENT_BUNDLE_BRANCH: main
    EDFI_STUDENT_ID_TYPES: Local,School,District,Test Taker ID

packages:
  ${ASSESSMENT_BUNDLE}:
    git: https://github.com/edanalytics/earthmover_edfi_bundles.git
    branch: ${ASSESSMENT_BUNDLE_BRANCH}
    subdirectory: assessments/${ASSESSMENT_BUNDLE}
  student_ids:
    local: ../student_ids

transformations:
  input:
    source: $transformations.input_xwalked
